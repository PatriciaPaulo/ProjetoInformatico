package com.example.splmobile.android.di

import android.content.Context
import com.example.splmobile.android.data.DataStoreRepository

/*
import dagger.Module
import dagger.Provides
import dagger.hilt.InstallIn
import dagger.hilt.android.qualifiers.ApplicationContext
import dagger.hilt.components.SingletonComponent
import javax.inject.Singleton

@Module
@InstallIn(SingletonComponent::class)
object OnboardingModel {

    //This object tells the system how to provide the DataStoreRepository
    @Provides
    @Singleton
    fun provideDataStoreRepository(
        //DataStoreRepository is provided through application context
        @ApplicationContext context: Context
    ) = DataStoreRepository(context = context)

}*/